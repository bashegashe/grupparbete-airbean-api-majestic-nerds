import { Router } from 'express';

const router = Router();

// router.post('/signup', ...);
// router.post('/login', ...);
// router.get('/history', ...);
// router.get('/status', ...);

export default router;
